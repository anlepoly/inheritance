BUG REPORT COMMENTS:
(In reply to Sam Davis from comment #49)
> I don't really see a use case for displaying a graph or table of query
> times. Instead, I think it could be useful to use the query history data to
> automatically adjust the query interval. But I don't think we should be
> using a core bundle to persist this information since core bundles should be
> stateless. Maybe we should come up with a design (both a UI design and an
> implementation design) before going farther with this.

OK,

the I stop with this and wait until someone tell me that we have the design.

